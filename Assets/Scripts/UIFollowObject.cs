using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class UIFollowObject : MonoBehaviour
{
    /*
    public Transform targetObject;
    public Vector3 screenOffset = new Vector3(0, 0, 0);
    public Vector3 uiBkgOffset = new Vector3(0, 0, 0);

    private RectTransform uiTextRectTransform;
    private RectTransform uiBkgRectTransform;

    // Start is called before the first frame update
    void Start()
    {
        uiTextRectTransform = GetComponentInChildren<Text>().GetComponent<RectTransform>();
        uiBkgRectTransform = GetComponentInChildren<Image>().GetComponent<RectTransform>();
    }

    void LateUpdate()
    {
        Vector3 screenPosition = Camera.main.WorldToScreenPoint(targetObject.position);
        uiTextRectTransform.position = screenPosition + screenOffset;
        uiBkgRectTransform.position = screenPosition + uiBkgOffset;
    }
    */
}
